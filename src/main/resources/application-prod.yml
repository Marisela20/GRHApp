server:
<<<<<<< HEAD
  port: 8080           # Railway internal port = 8080
=======
>>>>>>> 8d90520 (deploy a prod)
  address: 0.0.0.0
  port: ${PORT:8080}   # usa PORT que inyecta Railway, con 8080 por defecto

spring:
  datasource:
<<<<<<< HEAD
    # Usa SPRING_DATASOURCE_* si están definidos; si no, toma las vars de MySQL de Railway.
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST}:${MYSQLPORT:3306}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8}
=======
    # Usa SPRING_DATASOURCE_URL si lo defines; si no, toma las vars nativas de Railway (MYSQLHOST, etc.)
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST}:${MYSQLPORT:3306}/${MYSQLDATABASE}?sslMode=REQUIRED&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8}
>>>>>>> 8d90520 (deploy a prod)
    username: ${SPRING_DATASOURCE_USERNAME:${MYSQLUSER}}
    password: ${SPRING_DATASOURCE_PASSWORD:${MYSQLPASSWORD}}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
<<<<<<< HEAD
    hibernate:
      ddl-auto: none        # en prod, no validar/crear; ya alineamos el esquema
    open-in-view: false
    show-sql: false
=======
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: none
>>>>>>> 8d90520 (deploy a prod)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: true

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

logging:
  level:
    org.flywaydb: INFO
